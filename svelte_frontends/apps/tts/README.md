# Cloudflare config

### Name
monorepo-tts-frontend

### Build command
```sh
cd packages/ui && npm run build && cd ../../apps/tts && npm run build
```

### Build output directory
```sh
apps/tts/build
```

### Path
```sh
svelte_frontends
```

### Env variables
```sh
VITE_API_TARGET=burnysc2.xyz
```
