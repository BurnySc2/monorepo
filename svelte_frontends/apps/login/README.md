# Cloudflare config

### Name
monorepo-login-frontend

### Build command
```sh
cd packages/ui && npm run build && cd ../../apps/login && npm run build
```

### Build output directory
```sh
apps/login/build
```

### Path
```sh
svelte_frontends
```

### Env variables
```sh
VITE_API_TARGET=https://burnysc2.xyz
```
