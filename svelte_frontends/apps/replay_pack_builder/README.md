# Cloudflare config

### Name
monorepo-replay-pack-builder-frontend

### Build command
```sh
cd packages/ui && npm run build && cd ../../apps/replay_pack_builder && npm run build
```

### Build output directory
```sh
apps/replay_pack_builder/build
```

### Path
```sh
svelte_frontends
```

### Env variables
```sh
VITE_API_TARGET=burnysc2.xyz
```
